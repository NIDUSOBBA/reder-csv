package org.example.line;

import java.math.BigDecimal;

public class ProductLine extends AbstractLine {
    public ProductLine(String name, Integer quantity, BigDecimal unitPrice) {
        super(name, quantity, unitPrice);
    }
}
